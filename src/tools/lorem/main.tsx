import { useMemo, useState } from 'react'; import { copyText, ToolLayout } from '../../shared/components/ToolLayout'; import { renderTool } from '../../shared/toolEntry';
const words='lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua enim ad minim veniam quis nostrud exercitation ullamco laboris nisi aliquip ex ea commodo consequat duis aute irure reprehenderit voluptate velit esse cillum fugiat nulla pariatur excepteur sint occaecat cupidatat non proident sunt culpa qui officia deserunt mollit anim id est laborum'.split(' ');
function sentence(n:number,offset:number){const value=Array.from({length:n},(_,i)=>words[(i+offset)%words.length]).join(' ');return value[0].toUpperCase()+value.slice(1)+'.'}
function App(){const [type,setType]=useState('parágrafos');const [amount,setAmount]=useState(3);const [seed,setSeed]=useState(0);const result=useMemo(()=>type==='palavras'?Array.from({length:amount},(_,i)=>words[(i+seed)%words.length]).join(' '):type==='frases'?Array.from({length:amount},(_,i)=>sentence(12,i*7+seed)).join(' '):Array.from({length:amount},(_,i)=>sentence(45,i*11+seed)).join('\n\n'),[type,amount,seed]);return <ToolLayout name="Gerador de lorem ipsum" title={<>Texto para <em>preencher</em></>} description="Gere conteúdo fictício para layouts, protótipos e testes."><div className="form-grid"><div className="form-field"><label>Formato</label><select value={type} onChange={e=>setType(e.target.value)}><option>parágrafos</option><option>frases</option><option>palavras</option></select></div><div className="form-field"><label>Quantidade</label><input type="number" min="1" max="100" value={amount} onChange={e=>setAmount(Math.min(100,Math.max(1,Number(e.target.value))))}/></div></div><div className="form-field" style={{marginTop:20}}><label>Resultado</label><textarea readOnly value={result} style={{minHeight:260}}/></div><div className="button-row"><button className="primary-button" onClick={()=>setSeed(x=>x+7)}>Gerar outro</button><button className="secondary-button" onClick={()=>copyText(result)}>Copiar</button></div></ToolLayout>}renderTool(<App/>);
